'use client'
import {useEffect,useState} from 'react'; import {api} from '@/lib/api'; import {asArray,errorMessage} from '@/lib/types'; import type {LeaderboardEntry} from '@/lib/types';
export default function Leaderboard(){const [rows,setRows]=useState<LeaderboardEntry[]>([]);const [state,setState]=useState('Loading leaderboard…');useEffect(()=>{api.leaderboard().then(x=>{setRows(asArray<LeaderboardEntry>(x));setState('')}).catch(e=>setState(errorMessage(e)))},[]);return <main className="mx-auto min-h-screen max-w-5xl px-5 py-10"><a href="/" className="eyebrow">← FF / FANTASY</a><p className="eyebrow mt-10">Live standings</p><h1 className="section-title">Leaderboard.</h1>{state&&<p className="mt-8 text-muted-foreground">{state}</p>}<div className="mt-10 border-t border-border">{rows.map((row,i)=><div key={row.user_id||i} className="grid grid-cols-[56px_1fr_100px] items-center gap-4 border-b border-border py-5"><span className={i<3?'font-mono text-primary':'font-mono text-muted-foreground'}>#{row.rank||i+1}</span><strong className="font-mono uppercase">{row.username||'Player'}</strong><span className="text-right font-mono">{row.points??0} <small className="text-muted-foreground">PTS</small></span></div>)}</div></main>}
