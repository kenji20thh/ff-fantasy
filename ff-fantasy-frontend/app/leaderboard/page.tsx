"use client"
import { useEffect,useState } from "react"
import { Medal,Trophy } from "lucide-react"
import { api } from "@/lib/api"
import type { LeaderboardEntry } from "@/lib/types"
import { Card, ErrorBox, Loading, PageTitle } from "@/components/ui"
export default function LeaderboardPage(){const [data,setData]=useState<LeaderboardEntry[]>([]);const [error,setError]=useState<unknown>(null);useEffect(()=>{api.leaderboard().then(setData).catch(setError)},[]);return <main className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><PageTitle eyebrow="The climb" title="Leaderboard" description="Your fantasy team score is calculated by the Go backend from kills, assists, first blood and placement, with the captain receiving the multiplier."/><div className="mt-10">{error?<ErrorBox error={error}/>:data.length===0?<Loading label="Waiting for leaderboard data…"/>:<Card className="overflow-hidden"><div className="hidden grid-cols-[100px_1fr_180px] border-b border-white/10 px-6 py-4 text-xs font-bold uppercase tracking-[.18em] text-zinc-600 sm:grid"><span>Rank</span><span>Manager</span><span className="text-right">Points</span></div>{data.map((e,i)=><div key={e.fantasy_team_id} className="grid grid-cols-[60px_1fr_auto] items-center gap-3 border-b border-white/10 px-5 py-5 last:border-0 sm:grid-cols-[100px_1fr_180px] sm:px-6"><span className="flex items-center gap-2 font-mono font-bold text-zinc-500">{i<3?<Medal className="size-4 text-[#f4d35e]"/>:null}{String(e.rank).padStart(2,"0")}</span><span className="font-semibold">{e.username}</span><span className="text-right font-mono font-black text-[#f4d35e]">{e.points} pts</span></div>)}</Card>}</div><div className="mt-10 grid gap-4 sm:grid-cols-3"><Rule title="Kills" value="10 pts"/><Rule title="Assists" value="5 pts"/><Rule title="First blood" value="5 pts"/></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><Rule title="Placement" value="15 → 1 pts for places 1 → 10"/><Rule title="Captain" value="2× player points"/></div></main>}
function Rule({title,value}:{title:string,value:string}){return <Card className="p-5"><Trophy className="size-5 text-[#f4d35e]"/><p className="mt-5 text-xs uppercase tracking-[.18em] text-zinc-600">{title}</p><p className="mt-1 font-mono font-bold">{value}</p></Card>}
