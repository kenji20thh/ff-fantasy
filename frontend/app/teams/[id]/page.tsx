'use client'
import { useEffect,useState } from 'react'; import { useParams } from 'next/navigation'; import Link from 'next/link'; import { api } from '@/lib/api'; import { asArray,errorMessage } from '@/lib/types'; import type { Player,Team } from '@/lib/types';
export default function TeamDetail(){const {id}=useParams<{id:string}>();const [players,setPlayers]=useState<Player[]>([]);const [team,setTeam]=useState<Team|null>(null);const [error,setError]=useState('');useEffect(()=>{const teamId=Number(id);Promise.all([api.players(teamId),api.teams()]).then(([p,t])=>{setPlayers(asArray<Player>(p));setTeam(asArray<Team>(t).find(x=>x.id===teamId)||null)}).catch(e=>setError(errorMessage(e)))},[id]);return <main className="mx-auto min-h-screen max-w-5xl px-5 py-10"><Link href="/teams" className="eyebrow">← Teams</Link><h1 className="section-title mt-8">{team?.name||`Team ${id}`}</h1>{error&&<p className="mt-8 text-red-400">{error}</p>}<section className="mt-10 border-t border-border">{!error&&!players.length&&<p className="py-8 text-muted-foreground">Loading players or no players found.</p>}{players.map(player=><div key={player.id} className="flex items-center justify-between border-b border-border py-5"><span className="font-mono font-bold uppercase">{player.name}</span><span className="text-xs text-muted-foreground">Player #{player.id}</span></div>)}</section></main>}
